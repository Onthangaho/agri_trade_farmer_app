// lib/features/crops/presentation/screens/add_crop_screen.dart
/// Placeholder add crop screen opened from the floating action button.

import 'package:flutter/material.dart';

class AddCropScreen extends StatelessWidget {
  const AddCropScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Crop Listing'),
      ),
      body: const Center(
        child: Text('Crop form coming soon'),
      ),
    );
  }
}
